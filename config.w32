// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("stringcase", "for stringcase support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("stringcase", "enable stringcase support", "no");

if (PHP_STRINGCASE != "no") {
	EXTENSION("stringcase", "stringcase.c");
}

